package proyectointegrador.bidup.database;


import android.content.Context;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteOpenHelper;
//
//public class BaseDatos extends SQLiteOpenHelper implements Constants {
////    private static final String NOMBRE_BASE = "BidUp.db";
////    private static final int VERSION_BASE = 1;
////
////    public BaseDatos(Context ctx){
////        super(ctx,NOMBRE_BASE,null,VERSION_BASE);
////    }
////    @Override
////    public void onCreate(SQLiteDatabase sqLiteDatabase) {
////        //creo la tabla de usuario
////        sqLiteDatabase.execSQL("CREATE TABLE " + NOMBRE_BASE + "(" + _ID + " INTEGER PRIMARY KEY, "
////                + FIRST_NAME + " TEXT NOT NULL, "
////                + LAST_NAME +  " TEXT NOT NULL,"
////                + PRECIO + " INTEGER NOT NULL,"
////                + COORDX + " INTEGER NOT NULL,"
////                + COORDY + " INTEGER NOT NULL,"
////                + CODIGO +" INTEGER NOT NULL)");
////        sqLiteDatabase.execSQL("CREATE TABLE " + NOMBRE_TABLA_VERSION + "(" + _ID + " INTEGER PRIMARY KEY)");
////    }
//
//    @Override
//    public void onUpgrade(SQLiteDatabase sqLiteDatabase, int i, int i1) {
//
//    }
//}
